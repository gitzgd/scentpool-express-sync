# 生产运行与数据安全手册

## 2026-08-22 内存复发分析与第一阶段止血

- 2026-08-17 至 22 日的 Render 事件和指标显示，正确生产服务多次触及 512 MB 上限并由平台终止；失败在无打印的夜间也发生，因此打印不是唯一触发条件。
- 日报历史扩展增加了只追加审计触发器。电子面单工作线程原先在空队列时每 2 秒新建一次 SQLite 连接，每条连接都需要解析扩大后的 Schema；合成数据库复现确认这是持续内存增长的主要路径。
- 第一阶段不删除审计触发器、不迁移数据库、不修改订单内容。工作线程改为独占并复用一条 SQLite 连接，空闲轮询按 2、5、15、60 秒退避；新建或重试批次会立即唤醒，不牺牲正常下单响应。
- 合成库连续执行 20,000 次空队列检查时，显式关闭的一次性连接峰值增长约 944 KiB，工作线程复用连接约 64 KiB。此结果为本地对抗性测试，不代替部署后的 24 小时 Render 观察。
- 批量打印改为全局单任务；单次硬上限 50 单，PDF 子进程目标 192 MB、硬上限 224 MB。Web 主进程 RSS 达到 240 MB 时不启动新合并，并向操作员明确说明订单和待打印状态没有改变。

## 2026-07-19 内存事故结论

- Render Starter 实例上限为 512 MB。生产事件记录在 7 月 15 日至 19 日间多次出现 `Ran out of memory`。
- 生产数据库为 4,710,400 字节，共 482 张发货单、1,352 条商品明细和 282 条批次明细。数据库完整性检查通过。
- 数据库中的物流原始报文约 0.72 MB，下单原始报文约 0.91 MB，不足以造成 512 MB 内存耗尽。
- 系统没有把电子面单 PDF 保存进 SQLite 或 `/var/data`，只保存快递100返回的远程 URL 和必要状态。
- 旧批量打印会在 Web 进程中同时保留下载内容、pypdf 页面对象、合并缓冲区和 HTTP 响应副本。多线程处理后，glibc 的多个 64 MB malloc arena 没有及时归还，生产进程 RSS 最终常驻约 431 MB。
- 健康检查每几秒执行多表计数、发货后台重复加载全量订单，以及物流同步并发执行会继续抬高临时内存和数据库压力，但不是主要根因。

## 已实施的保护

1. PDF 下载、解析与合并迁移到短生命周期子进程；源文件和结果均落在临时目录，主进程只流式发送合并文件。
2. PDF 子进程在 Linux 上目标限制为 192 MB、硬上限 224 MB，退出后操作系统立即回收全部内存；单次最多 50 单、源文件合计最多 60 MB。
3. Web 服务改用 8 个固定工作线程，避免 `ThreadingHTTPServer` 无上限创建线程和 malloc arena。
4. Render 设置 `MALLOC_ARENA_MAX=2` 与 `MALLOC_TRIM_THRESHOLD_=131072`。
5. 健康检查改成单条 `SELECT 1`；成功的健康检查不再写访问日志。
6. 状态卡片改走 SQL 聚合接口，不再为统计数字额外下载一次全部订单。
7. 同步物流与同步退货物流均增加进程锁，避免两个全量任务同时运行；响应只返回计数，不在内存中保存每单完整结果。
8. SQLite 使用 WAL、15 秒 busy timeout、2 MB 页缓存和文件型临时表；列表 API 不再返回下单原始报文、回调盐和轮询令牌。
9. 数据库下载与管理命令统一使用 SQLite Backup API，并在生成后执行完整性检查。
10. 电子面单空闲工作线程复用一条专用 SQLite 连接并退避等待；新批次通过进程内事件即时唤醒。
11. 批量打印全局串行；Web 主进程达到 240 MB 时拒绝启动新的 PDF 子进程，失败前不修改打印状态。

## 数据保留与恢复

### 当前基线

- 事故处理前备份：`scentpool-backup-20260719-233735.db`
- 大小：4,710,400 字节
- SHA-256：`518241cd6f0d96eb2f9618e6a00d7d5152e46a3e2b4821bc5a08bbac49665821`
- `PRAGMA integrity_check`：`ok`

备份含收件人信息，只能存放在受控设备或加密对象存储中，不能提交到 Git。

### 备份策略

- 第一层：Render 持久磁盘每天自动快照，至少保留 7 天。
- 第二层：每天下载一份 SQLite 在线备份到异地对象存储，保留 30 个日备份和 12 个月备份。
- 第三层：每月从异地备份恢复到临时数据库，核对完整性和发货单、商品明细、批次明细数量。
- 发布前：必须先下载并校验一份备份；数据库结构迁移后再次备份。

恢复顺序：停止写入或进入维护窗口，下载目标备份，在临时路径执行完整性检查，使用受控恢复接口替换数据库，重启后核对诊断接口和最近 20 张订单，再关闭恢复开关。

## 容量预估

当前整库平均占用约 9.8 KB/单，已包含商品、索引、批次和现有物流报文。为了覆盖更长物流轨迹、WAL、索引增长和临时空间，规划按 20 KB/单计算。

| 场景 | 新增订单 | 原始增量估计 | 规划增量（20 KB/单） |
| --- | ---: | ---: | ---: |
| 常态一天 | 80 | 0.78 MB | 1.6 MB |
| 峰值一天 | 200 | 1.95 MB | 4.0 MB |
| 常态一年 | 29,200 | 285 MB | 584 MB |
| 峰值一年 | 73,000 | 713 MB | 1.46 GB |

当前 1 GB 磁盘在常态流量下按安全口径约 1.2 年到达 70%，若长期每天 200 单则约 6 个月。建议近期把 Render 磁盘扩到 5 GB；按当前公开单价，5 GB 持久磁盘约 1.25 美元/月，且扩容后不能缩小。

## 数据库路线

### 当前阶段：继续 SQLite

每天 80 至 200 单对 SQLite 很轻。当前问题是 PDF 内存路径，不是数据库容量或查询性能。保留 SQLite 可以避免一次高风险迁移，成本也最低。需要把磁盘扩到 5 GB并落实异地备份。

满足任一条件时启动 PostgreSQL 迁移：

- 数据库文件超过 2 GB或磁盘使用率连续一周超过 60%；
- 一天出现 5 次以上 `database is locked`；
- Web 服务需要运行两个或更多实例；
- 需要高可用、只读报表、跨服务共享数据库或分钟级恢复点。

### 首选迁移：Render PostgreSQL

与 Web 服务同在新加坡区，网络和运维最简单。迁移时新增 PostgreSQL 数据访问层，先做 SQLite 全量导入，再做增量校验和短维护窗口切换。Render 的付费 PostgreSQL 提供 PITR；存储按 0.30 美元/GB/月计费。

### 国内厂商选项：阿里云 RDS PostgreSQL Serverless（新加坡）

这是国内厂商、海外同区域的折中方案。新加坡公开价为 0.0796 美元/RCU 小时和 0.0004 美元/GB 小时；若 0.5 RCU 持续运行，计算约 29 美元/月，另计存储。可自动暂停的低频环境成本会下降，但生产站点的持续访问可能使其长期保持运行。跨 Render 公网连接还要增加 TLS、IP 白名单、连接池和网络故障处理，因此只有在国内厂商偏好高于最低成本和最低复杂度时采用。

不建议把 Render 新加坡 Web 服务直连中国大陆数据库，跨境链路的延迟和稳定性会放大每个页面请求；如必须使用大陆数据库，应把应用服务一并迁到同一云和同一区域。

## 监控与告警

- 每 5 分钟读取 Render 的内存与重启事件；内存 5 分钟超过 70%告警，超过 85%立即停止大批打印并检查诊断接口。
- 每天记录 `/api/admin/system/diagnostics` 的 RSS、峰值 RSS、线程数、数据库/WAL 大小和表数量。
- 每天核对总部发货后台“异常提醒”或总部权限接口 `/api/admin/task-alerts`；记录面单下单、物流查询、退货物流查询、打印失败和等待超过 30 分钟的任务。该接口只返回业务编号、门店、分类、状态、错误摘要和异常版本，不返回收件人姓名、电话或地址。人工确认只隐藏当前异常版本，不代表修改或修复订单状态。
- “物流服务异常”代表快递100接口层故障，而不是每个单号都填错。系统会在首个失败后停止本轮批量查询并合并显示影响数量；此时不要逐单改号或连续点击同步。先核对快递100账号权限、余额、IP/访问限制和服务状态，等待下一轮探测。历史受影响订单只能由各自后续查询结果恢复，不执行批量清除失败状态。
- 磁盘超过 60%安排扩容或归档，超过 75%停止大文件操作。
- 批量打印失败时先把选择数量减半；不要连续重复提交同一批次。页面提示“已有面单正在生成”时等待当前下载完成；提示“网站当前内存较高”时等待几分钟再试，两个提示都不代表订单或面单被删除。
- 面单任务中断超过 10 分钟时，后台会每分钟检查并自动重新排队；单项累计尝试达到 3 次后停止自动恢复并明确标记失败。操作员应按页面提示检查配置、网点或余额，再使用“重新提交该批次失败订单”，不要重复创建新批次。
- 部署后预期空闲 RSS 应明显低于 200 MB。如果新实例空闲 30 分钟后仍高于 250 MB，应回滚并保留事件日志。

### 每日固定只读采集

- 固定采集器的仓库版本为 `tools/scentpool_daily_audit_probe.py`，本机副本为 `~/.codex/scentpool_daily_audit_probe.py`；只用 `scripts/install_daily_audit_probe.py` 原子更新并校验，禁止直接粘贴含令牌的命令版本。
- Render 目标固定为 `srv-d913padckfvc73eom3f0` / `scentpool-express-sync-ec7c`。采集器只允许健康检查、脱敏日报、服务详情、部署、事件、日志、内存、内存上限、磁盘使用/容量、HTTP 请求量和延迟的 GET 白名单；目标身份不一致时停止深度采集。
- `scentpool-audit-token` 与 `scentpool-render-api` 只从 Mac 登录钥匙串读入内存。不得改为命令参数、环境变量、仓库配置、截图或普通日志。
- 日志正文只在内存中匹配 OOM、5xx、异常栈、`database is locked`、timeout 和 `[slow-request]`；输出只保留聚合计数。Render 日志使用时间游标分页，部署/事件使用资源游标分页；游标缺失、没有前进或超过安全页数时必须明确标为结构变化/不完整。
- 每个通道必须返回明确状态：`ok`、`no_data`、`http_error`、`permission_denied`、`schema_changed`、`process_error`、`network_restricted` 或 `target_mismatch`。即使凭据读取或进程失败，也必须先输出一份有效脱敏 JSON，再以非零状态结束。
- Render 工作区套餐可能不提供 HTTP 请求日志或响应延迟指标；应保留对应 `no_data`/HTTP 错误状态，不能把缺少该通道写成“0 次 5xx”或“延迟正常”。5xx 优先使用按状态码聚合的 HTTP 请求指标，日志计数只标为 request-log 证据。

## 发布验收

1. 确认事故前备份仍在本地且 SHA-256 一致。
2. 部署后访问健康检查和管理员诊断接口，确认 `journal_mode=wal`、记录数量不变。
3. 依次测试 1、20、50 张面单合并，观察每次完成后主进程 RSS 是否回落。
4. 核对 3 张历史订单的商品、快递单号、物流轨迹和打印状态。
5. 连续观察 24 小时；无 OOM 且空闲内存稳定后，保留单次 50 张硬上限，不在 512 MB 实例恢复 100 至 200 张打印。
6. 部署日报历史扩展前执行 SQLite 在线备份并对备份运行 `PRAGMA integrity_check`；部署后确认 `audit_event_meta`、`audit_state_events` 和只追加保护存在，且 `completeness.full_day_coverage_from` 不早于真实完整证据起点。
