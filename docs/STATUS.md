# 当前状态

更新时间：2026-08-03

本文件区分“仓库已验证事实”和“外部生产待核实状态”。历史任务中的监控结果会过期，不能直接当作当前结论。

## 仓库基线

- Git 仓库：`/Users/zgd/scentpool-express-sync`。
- GitHub：`gitzgd/scentpool-express-sync`。
- GitHub 可见性：公开仓库，于 2026-08-03 通过 GitHub CLI 核实。
- 默认分支：`main`。
- 治理初始化前基线：`392ac08`（`备注UI设计调整`）。
- 治理初始化时 `main` 与 `origin/main` 一致，工作区无未提交文件。

## 已验证代码能力

- 门店发货、发货看板、退货和权限隔离。
- 总部订单、退货、门店、商品和电子面单设置。
- CSV/XLSX 导出和商品分类换行。
- 快递100物流查询、签收同步和手动刷新。
- 快递100电子面单批量下单、取消保护、复打和打印状态。
- 电子面单短暂故障幂等重试、中断任务限次恢复，以及总部可见的任务失败原因和处理建议。
- 批量面单 PDF 子进程合并与内存限制。
- SQLite WAL、在线备份、恢复保护、完整性检查和系统诊断。
- Render 配置、持久磁盘和健康检查。
- 综合烟测覆盖主要业务与历史迁移。

## 生产历史观察

以下内容来自历史任务记录；每条观察只代表注明日期，不能直接当作当前实时状态：

- 历史上正确生产服务被记录为 `scentpool-express-sync-ec7c`。
- 历史上还存在一个由相同仓库触发部署的重复 Render 服务 `scentpool-express-sync`，该副本启动失败并发送邮件。
- 2026-07-24 的历史巡检记录称健康检查、SQLite 完整性和自动物流同步正常。
- 历史巡检记录称内存优化部署后未再次出现 512 MB OOM，内存约为 319–342 MB。
- 历史记录中的数据库大小从 4.7 MB 增长到约 8.1 MB；这些数值会继续变化。
- 2026-08-03 每日盘点任务对 2026-08-02 的只读检查记录：当日新增 50 单、发货 40 单、签收 6 单；观察到当日新增 1 项电子面单失败、检查时共有 4 项面单失败。两次批量打印请求约为 2.6 秒和 15.9 秒，未观察到打印失败；检查时没有等待超过 30 分钟的面单任务。健康检查正常，未观察到 OOM、5xx、Traceback、数据库锁或超时。
- 同一盘点任务随后对上述 4 项当前记录作只读分类：2 项是快递100 `30011` 已返回完整面单但被旧逻辑误判，1 项为承运区域停发/超出范围，1 项为地址行政区解析失败；未发现这些记录由删除订单产生。该观察不代表部署后已自动清理历史状态。

以上只能作为下一次巡检的比较基线。

## 待核实事项

### P0：公开仓库安全

- 重新扫描完整 Git 历史，确认没有生产密钥、数据库、顾客信息或可用凭据曾被提交。
- 如果历史中存在敏感信息，普通删除当前文件不够，需要轮换凭据并专项清理 Git 历史。

### P0：Render 服务唯一性

- 核实当前生产服务的准确 Service ID、URL、区域和分支。
- 核实错误的重复服务和 Blueprint 是否仍存在。
- 未确认准确目标前，不执行删除或环境变量修改。

### P1：电子面单开关

- `render.yaml` 当前声明 `SCENTPOOL_KUAIDI100_LABEL_ENABLED=1`。
- `README.md` 的说明仍包含“测试完成前保持 0”和“测试后再改为 1”的历史流程。
- 需要核实生产是否已完成真实测试并正式启用，再统一说明。

### P1：磁盘扩容口径

- `OPERATIONS.md` 的事故期规划建议近期从 1 GB 扩到 5 GB。
- 后续历史巡检根据当时约 8.1 MB 的数据库判断无需立即扩容，可在磁盘使用率达到 60% 时安排。
- 应根据 Render 当前磁盘用量、订单增速、备份空间和 WAL 峰值更新单一口径。

### P1：备份与恢复演练

- 核实 Render 自动快照是否持续成功。
- 核实异地备份是否真实执行，而不只是文档建议。
- 记录最近一次从备份恢复到临时数据库并执行完整性检查的日期。

## 建议的下一批任务

1. `10｜生产运维｜Render・SQLite・备份`：只读核实上述外部状态。
2. `安全｜扫描公开仓库历史中的敏感信息`：检查 Git 全历史并给出轮换建议。
3. `文档｜统一电子面单与磁盘扩容口径`：以巡检证据更新 README、OPERATIONS 和配置注释。
4. 后续功能或修复：一项需求一个 Worktree 和分支。

## 状态更新规则

- 代码合并后更新仓库基线和功能状态。
- 每次生产巡检写明日期、证据来源和是否执行了外部操作。
- 不能确认的项目保留为“待核实”，不要用旧任务结论覆盖。
