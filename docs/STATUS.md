# 当前状态

更新时间：2026-08-22

本文件区分“仓库已验证事实”和“外部生产待核实状态”。历史任务中的监控结果会过期，不能直接当作当前结论。

## 仓库基线

- Git 仓库：`/Users/zgd/scentpool-express-sync`。
- GitHub：`gitzgd/scentpool-express-sync`。
- GitHub 可见性：公开仓库，于 2026-08-03 通过 GitHub CLI 核实。
- 默认分支：`main`。
- 治理初始化前基线：`392ac08`（`备注UI设计调整`）。
- 治理初始化时 `main` 与 `origin/main` 一致，工作区无未提交文件。

## 已验证代码能力

- 门店发货、发货看板、退货和权限隔离。
- 总部订单、退货、门店、商品和电子面单设置。
- CSV/XLSX 导出和商品分类换行。
- 快递100物流查询、签收同步和手动刷新。
- `main` 已包含退货快递公司自动识别热修复：门店只填单号，系统保存并直接使用快递100官方公司编码；申通等电子面单列表以外的承运商不再被三家公司白名单拦截，历史人工记录会重新识别，失败原因和处理建议持续可见。
- 快递100电子面单批量下单、取消保护、复打和打印状态。
- 电子面单短暂故障幂等重试、中断任务限次恢复，以及总部可见的任务失败原因和处理建议。
- 批量面单 PDF 子进程合并与内存限制。
- SQLite WAL、在线备份、恢复保护、完整性检查和系统诊断。
- Render 配置、持久磁盘和健康检查。
- 综合烟测覆盖主要业务与历史迁移。

退货自动识别验证说明：Python 编译、前端 JavaScript 语法检查和完整 `smoke_test.py` 已通过。使用独立合成数据库完成约 1440 px 桌面和 390 px 手机验收：新增退货页没有快递公司选择框、自动识别说明可见；退货看板覆盖京东纠正成功、无法识别的长中文失败提示、顺丰签收、多商品和长单号，页面根级无横向溢出且浏览器错误日志为空。

退货自动识别部署说明：PR #3 已合并到 `main`，合并提交 `38f7807` 已部署到生产；2026-08-05 的生产看板错误记录已显示快递100返回申通候选，确认生产账号可以调用智能单号识别接口。

退货承运商编码热修复说明：PR #5 已合并到 `main`，合并提交 `2ee1c5f`。Python 编译、前端 JavaScript 语法检查和完整 `smoke_test.py` 均通过；对抗性覆盖包含申通 `shentong`、非法公司编码、旧库增量迁移和历史错误公司纠正。

## 内存稳定性第一阶段已上线

- 2026-08-22 只读生产证据显示，正确服务 `scentpool-express-sync-ec7c` 在 2026-08-17 至 22 日多次触及 512 MB 内存上限并重启；多数重启前后没有批量打印，因此打印只是放大因素。
- 代码根因复现为：电子面单空队列每 2 秒创建 SQLite 连接，新增审计触发器扩大 Schema 后反复解析造成常驻内存增长。第一阶段改为工作线程独占单连接并按 2、5、15、60 秒退避，新建/重试批次即时唤醒。
- 批量打印改为单任务，单次硬上限 50 单；PDF 子进程目标 192 MB、硬上限 224 MB；Web 主进程达到 240 MB 时在读取面单和启动子进程前返回明确提示，不修改订单或打印状态。
- 合成库 20,000 次空队列对抗性测试中，一次性连接峰值增长约 944 KiB，复用连接约 64 KiB。完整 Python 编译、前端 JavaScript 语法检查和 `smoke_test.py` 已通过；覆盖超量、并发和高内存拒绝路径。
- 提交 `7caa109` 已合并并推送到 `main`。2026-08-22，正确生产服务 `scentpool-express-sync-ec7c`（Service ID `srv-d913padckfvc73eom3f0`）的部署 `dep-da4q03d6d1lc73853c70` 标记为 `live`；历史重复服务未操作。
- 部署前通过总部会话下载 SQLite 在线备份到本机受控目录，权限 `0600`，`PRAGMA integrity_check` 返回 `ok`；上线前后脱敏业务汇总计数一致。部署后按最新时间点对齐 Render 指标，活动实例 RSS 约 95.9 MiB、实例上限 512 MiB。
- 本阶段没有 Schema 迁移、生产订单状态批量改写或真实面单请求。即时验证通过，但仍需连续观察 24 小时确认没有再次触发 OOM。

## 发货列表服务端分页（本地已验证，生产待部署）

- `GET /api/shipments` 增加 `page` / `page_size`，每页硬上限 50 单；查询先在 SQLite 统计总数，只读取当前页订单及其商品明细。越界页码回退到最后一页，非法页码返回明确中文错误。
- 总部和门店页面切页时只刷新当前页，不重复读取商品、门店、面单设置和异常提醒；筛选后的状态汇总仍使用 SQL 聚合接口。
- CSV/XLSX 导出和电子面单批次预览继续读取全部匹配记录，不受页面 50 单限制。合并打印明确限定“当前页”，避免误以为跨页全选。
- 使用部署前生产备份的受控副本、只输出聚合性能数字：2673 单全量读取中位约 230.7 ms、Python 分配峰值约 15.3 MiB；第一页 50 单约 3.8 ms、约 0.2 MiB。该结果不包含公网传输和浏览器渲染，但证明服务端物化量已被限制。
- 完整烟测覆盖两页、越界回退、非法参数、门店隔离、完整导出和完整批量预览。约 1440 px 桌面与 390 px 手机合成数据验收通过，包含长业务 ID、长地址、长备注和多商品；页面根级无横向溢出，浏览器错误日志为空。
- 本阶段没有 Schema 迁移，也不修改现有订单、面单或物流状态。生产部署前继续使用第一阶段的有效备份基线。

## 脱敏只读业务日报已上线

- `main` 提交 `74066c1` 已实现并上线脱敏只读业务日报接口：独立 Bearer 鉴权、严格上海时区日期、SQLite URI `mode=ro` + `PRAGMA query_only=ON`、只返回允许的汇总字段。
- 专项合成数据测试覆盖日期边界、门店汇总、7 日均值、异常与长等待、数据质量、递归隐私检查、令牌不进入响应或日志、总部会话与审计令牌双向隔离，以及只读连接拒绝写入。
- 2026-08-05 19:22（Asia/Shanghai），正确生产服务 `scentpool-express-sync-ec7c` 的部署 `dep-d9phphoae00c73d5s980` 将 `74066c1` 标记为 `live`；公开健康检查返回 `ok=true`、`database=true`。
- 生产只读验收确认：正确令牌返回脱敏汇总；缺失或错误令牌返回 `401`；非 `GET` 返回 `405`；日报令牌访问普通管理员诊断接口仍返回 `401`；递归隐私扫描通过。`SCENTPOOL_AUDIT_TOKEN` 仅保存在正确生产服务环境变量和 Mac 登录钥匙串中，未进入仓库、日志或任务输出。
- 同仓库的历史重复服务 `scentpool-express-sync` 仍存在，但本次没有修改或部署该副本。

## 日报历史与固定采集器修复已上线

- 2026-08-17，提交 `a25501b` 已推送到 `main`。正确生产服务 `scentpool-express-sync-ec7c`（Service ID `srv-d913padckfvc73eom3f0`）的部署 `dep-da17chh42hec73ag7d70` 于 10:48（Asia/Shanghai）标记为 `live`；历史重复服务未操作。
- 新增只含内部整数记录号、规范化状态/原因分类和时间的 `audit_event_meta` / `audit_state_events`。迁移保存一次脱敏旧库快照，此后由触发器只追加并拒绝更新/删除；不保存业务编号、订单号、快递单号、姓名、电话、地址、密钥、异常原文或第三方原始报文。
- 日报扩展提供目标日新建且日末未发出、日末已发货未签收、待处理超过 24 小时、面单/打印超过 30 分钟，以及面单/物流/打印的目标日新失败、当前未恢复和历史残留。只有 `full_day_coverage_from` 起的完整自然日使用事件精确复原；更早日期明确返回部分口径和限制。
- 仓库新增固定 GET 白名单采集器及原子安装器；本机 `/Users/zgd/.codex/scentpool_daily_audit_probe.py` 已更新并校验为 `0700`，令牌继续只从 Mac 登录钥匙串读入内存。
- 2026-08-15 对 2026-08-14 的一次生产只读验证确认：健康、现有日报、正确 Render 服务身份、日志及分页、内存/上限、磁盘使用/容量、HTTP 请求量均返回结构化状态；目标日部署和平台事件明确返回 `no_data`，不再出现成功退出但无结果。HTTP 延迟指标使用多分位和单分位参数均返回 HTTP 400，当前仍记为通道级 HTTP 错误，不能据此宣称延迟正常；本次没有执行部署、环境变量修改或生产数据库写入。
- 本地验证使用合成数据；2026-08-16 最终复跑完整 Python 编译、前端 JavaScript 语法检查、日报/采集器专项测试和完整 `smoke_test.py` 均通过。
- 部署前通过总部会话下载 SQLite 在线备份到本机受控目录，文件权限为 `0600`，`PRAGMA integrity_check` 返回 `ok`；备份文件、摘要和任何客户数据均未进入仓库。
- 2026-08-17 上线后公开 `/api/health` 返回 `ok=true`、`database=true`。固定采集器对 2026-08-16 的生产只读验证确认 `historical_end_of_day`、脱敏 `failures`、`completeness` 和 `long_waiting` 均已返回；`full_day_coverage_from=2026-08-18`，因此 2026-08-16 明确标为部分证据，不能当作完整历史日末。Render 日志接口本次返回 HTTP 503、延迟指标仍返回 HTTP 400，采集器据此保持总状态 `error`，没有把缺失的运维证据误报为正常。

## 异常提醒中心已上线

- 总部发货后台已把默认展开的“任务状态”大面板改为页头异常角标和弹窗；弹窗按电子面单、物流查询、打印分类，并标出系统继续检查或需要人工处理。
- 异常接口每 60 秒独立刷新，不重新加载订单表；底层失败字段恢复后提醒自动消失。`POST /api/admin/task-alerts/resolve` 可确认当前异常版本，不修改订单、快递单号、面单或物流状态；同一问题再次发生会因版本指纹变化重新提醒。
- 增量表 `task_alert_acknowledgements` 只保存异常标识、类型、业务记录 ID、版本指纹、确认用户 ID 和时间，不保存收件人、电话、地址、订单号、快递单号或第三方原始报文。
- 2026-08-06 本地验证：完整 Python 编译、前端 JavaScript 语法检查和 `smoke_test.py` 通过。对抗性覆盖门店越权 `403`、过期指纹 `409`、人工确认后隐藏、新失败重新出现、底层恢复后自动消失和隐私字段排除。使用 3 项合成异常完成约 1440 px 桌面和 390 px 手机验收：分类切换、键盘关闭与焦点回位、弹窗内滚动、长业务 ID、无横向溢出均通过，浏览器错误日志为空；60 秒刷新后未保存的发货备注保持不变。
- PR #7 已合并到 `main`，合并提交为 `5629131`。2026-08-06 23:00（Asia/Shanghai），正确生产服务 `scentpool-express-sync-ec7c` 的部署 `dep-d9qa2s3bc2fs7397gar0` 将该提交标记为 `live`。
- 上线后公开只读验证确认：`/api/health` 返回 `ok=true`、`database=true`，生产 `static/app.js` 包含异常提醒入口、人工确认接口和 60 秒刷新逻辑；未登录访问异常列表或提交人工确认均返回 `401`。本次没有读取、修改或确认任何真实生产异常，历史重复服务也未操作。

## 物流平台异常聚合热修复

- 2026-08-07 只读核对正确生产服务 `scentpool-express-sync-ec7c`：物流服务商为快递100，自动查询已开启，发货查询间隔为 360 分钟，实时查询地址为 `https://poll.kuaidi100.com/poll/query.do`，customer/key 均已配置；没有读取或输出密钥内容。
- 生产截图中的 50 余条 `HTTP Error 403: Forbidden` 来自同一轮接口层拒绝，不是“未揽收”。仓库原有无轨迹分支会返回“等待揽收”；原批处理缺少系统故障熔断，因而把一次平台故障写成了多条订单失败。
- 热修复将 HTTP `401/403/408/429/5xx`、连接失败和无效响应分为系统级故障：本轮只探测一个单号后停止，不覆盖订单已有物流状态；异常中心把同一故障合并为一条并显示影响数量。历史失败不会批量重置，只有对应单号后续查询成功才恢复。
- 电子面单分类改用独立列表，修复“角标有 4 项但打开为空”；同时修复订单因另一个异常被选中时，尚未超过 30 分钟的排队面单被误算为等待过久的问题。
- 本地完整烟测已通过；对抗性覆盖 HTTP 403 分级、两笔候选只发出一次请求、业务状态保持不变、51 条系统错误合并为一项、分类防饥饿和增量表迁移。3 项合成异常在约 1440 px 桌面与 390 px 手机验收通过：分类按钮和操作按钮完整可见、无横向溢出、浏览器错误日志为空。
- PR #9 已合并到 `main`，合并提交为 `70d9d2a`。2026-08-07 01:30（Asia/Shanghai），正确生产服务 `scentpool-express-sync-ec7c`（Service ID `srv-d913padckfvc73eom3f0`）的部署 `dep-d9qc95gu01pc739c9250` 将该提交标记为 `live`；公开 `/api/health` 返回 `ok=true`、`database=true`，生产 `static/app.js` 已包含平台故障熔断、分类独立列表和“无需逐单处理”提示，未登录访问异常列表仍返回 `401`。本次没有登录生产后台、触发物流同步、修改真实订单或操作历史重复服务。

## 生产历史观察

以下内容来自历史任务记录；每条观察只代表注明日期，不能直接当作当前实时状态：

- 历史上正确生产服务被记录为 `scentpool-express-sync-ec7c`。
- 历史上还存在一个由相同仓库触发部署的重复 Render 服务 `scentpool-express-sync`，该副本启动失败并发送邮件。
- 2026-07-24 的历史巡检记录称健康检查、SQLite 完整性和自动物流同步正常。
- 历史巡检记录称内存优化部署后未再次出现 512 MB OOM，内存约为 319–342 MB。
- 历史记录中的数据库大小从 4.7 MB 增长到约 8.1 MB；这些数值会继续变化。
- 2026-08-03 每日盘点任务对 2026-08-02 的只读检查记录：当日新增 50 单、发货 40 单、签收 6 单；观察到当日新增 1 项电子面单失败、检查时共有 4 项面单失败。两次批量打印请求约为 2.6 秒和 15.9 秒，未观察到打印失败；检查时没有等待超过 30 分钟的面单任务。健康检查正常，未观察到 OOM、5xx、Traceback、数据库锁或超时。
- 同一盘点任务随后对上述 4 项当前记录作只读分类：2 项是快递100 `30011` 已返回完整面单但被旧逻辑误判，1 项为承运区域停发/超出范围，1 项为地址行政区解析失败；未发现这些记录由删除订单产生。该观察不代表部署后已自动清理历史状态。
- 2026-08-03 22:42（Asia/Shanghai），通过 Render Dashboard 核实正确生产服务 `scentpool-express-sync-ec7c` 已将 `main` 合并提交 `38f7807` 标记为 live；公开 `/api/health` 返回 `ok=true`、`database=true`，生产 `static/app.js` 已包含退货公司自动识别结果提示。此次只做部署观察和公开端点验证，没有创建或修改真实退货记录。
- 2026-08-05 14:45（Asia/Shanghai），GitHub Deployment 状态显示正确生产服务 `scentpool-express-sync-ec7c` 已成功部署 `main` 合并提交 `2ee1c5f`；14:50 公开 `/api/health` 返回 `ok=true`、`database=true`。同一提交在历史重复服务 `scentpool-express-sync` 的 Deployment 仍失败，本次未操作该副本，也未查询或修改真实退货记录。

以上只能作为下一次巡检的比较基线。

## 待核实事项

### P0：公开仓库安全

- 重新扫描完整 Git 历史，确认没有生产密钥、数据库、顾客信息或可用凭据曾被提交。
- 如果历史中存在敏感信息，普通删除当前文件不够，需要轮换凭据并专项清理 Git 历史。

### P0：Render 服务唯一性

- 核实当前生产服务的准确 Service ID、URL、区域和分支。
- 核实错误的重复服务和 Blueprint 是否仍存在。
- 未确认准确目标前，不执行删除或环境变量修改。

### P1：电子面单开关

- `render.yaml` 当前声明 `SCENTPOOL_KUAIDI100_LABEL_ENABLED=1`。
- `README.md` 的说明仍包含“测试完成前保持 0”和“测试后再改为 1”的历史流程。
- 需要核实生产是否已完成真实测试并正式启用，再统一说明。

### P1：磁盘扩容口径

- `OPERATIONS.md` 的事故期规划建议近期从 1 GB 扩到 5 GB。
- 后续历史巡检根据当时约 8.1 MB 的数据库判断无需立即扩容，可在磁盘使用率达到 60% 时安排。
- 应根据 Render 当前磁盘用量、订单增速、备份空间和 WAL 峰值更新单一口径。

### P1：备份与恢复演练

- 核实 Render 自动快照是否持续成功。
- 核实异地备份是否真实执行，而不只是文档建议。
- 记录最近一次从备份恢复到临时数据库并执行完整性检查的日期。

## 建议的下一批任务

1. `10｜生产运维｜Render・SQLite・备份`：只读核实上述外部状态。
2. `安全｜扫描公开仓库历史中的敏感信息`：检查 Git 全历史并给出轮换建议。
3. `文档｜统一电子面单与磁盘扩容口径`：以巡检证据更新 README、OPERATIONS 和配置注释。
4. 后续功能或修复：一项需求一个 Worktree 和分支。

## 状态更新规则

- 代码合并后更新仓库基线和功能状态。
- 每次生产巡检写明日期、证据来源和是否执行了外部操作。
- 不能确认的项目保留为“待核实”，不要用旧任务结论覆盖。
